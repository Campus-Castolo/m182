services:
  client:
    hostname: forensic-client
    image: alpine:latest
    container_name: forensic-client
    command: sh -c "apk add --no-cache lftp curl bind-tools netcat-openbsd busybox-extras && sleep infinity"
    networks:
      forensic-net:
        ipv4_address: 192.168.10.50
    tty: true
    stdin_open: true

networks:
  forensic-net:
    ipam:
      config:
        - subnet: 192.168.10.0/24
